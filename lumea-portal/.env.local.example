# ── BFF: backend URLs (server-side only — never sent to the browser) ──────────
AUTH_API_URL=http://localhost:8080
CONTENT_API_URL=http://localhost:8788
POST_API_URL=http://localhost:8787
INTERACTION_API_URL=http://localhost:8790
ANALYTICS_API_URL=http://localhost:8789
NOTIFICATIONS_API_URL=http://localhost:8792
AI_API_URL=http://localhost:8791
MEDIA_API_URL=http://localhost:8793

# ── BFF: API key injected into every proxied request (server-side only) ───────
# Use key #1 for portal traffic so you can identify it in backend logs.
# Keep this out of source control.
API_KEY=cb260c59e34ca2a45868c9e

# ── Public (safe to expose) ───────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
